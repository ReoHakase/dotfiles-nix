# Raycast

`~/.config/raycast/extensions` is large (hundreds of MB from the Store) and is not tracked here. Install or sync extensions per machine from Raycast.

Optional: back up `extensions` elsewhere or document a restore procedure.
